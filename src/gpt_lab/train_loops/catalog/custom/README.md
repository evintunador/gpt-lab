### custom train loops
place any entirely custom training loops here.
currently these loops must still abide by the same rules (tests) as those in `atomic_features/` and `llm_compiled/`

### todo
- [ ] either remove the strict test requirements entirely or make them optional, not sure yet