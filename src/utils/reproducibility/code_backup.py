"""
build a tool that walks the imports, 
checks for those which are from this repo, 
grabs the files they come from,
and adds those files (along with proper directory structure) to the model's saved backup/checkpoint folder
maybe also a couple .py files that aren't imported but i for some reason know will be useful
"""